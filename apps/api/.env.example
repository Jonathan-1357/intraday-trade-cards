DATABASE_URL=sqlite:///./trade_cards.db
CORS_ORIGINS=["http://localhost:3000"]
DEFAULT_CAPITAL=100000.0
DEFAULT_RISK_PER_TRADE=1000.0

# Upstox live market data (optional — mock data used if blank)
# Run scripts/upstox_auth.py to generate this token each trading day
UPSTOX_ACCESS_TOKEN=
